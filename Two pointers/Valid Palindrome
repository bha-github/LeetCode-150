class Solution:
    def isPalindrome(self, s: str) -> bool:
        str1 = ""
        s = s.lower()
        for i in s:
            if i.isalnum():
                str1 += i
        return str1 == str1[::-1]
